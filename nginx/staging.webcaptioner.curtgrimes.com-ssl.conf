server {
    listen 443 ssl;
    listen [::]:443;
    server_name staging.webcaptioner.example.com www.staging.webcaptioner.example.com;

    ssl_certificate /etc/letsencrypt/live/staging.webcaptioner.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.webcaptioner.example.com/privkey.pem;

    root /opt/staging.webcaptioner.example.com/public;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
